# Copy this file to `.env` and replace with your GA4 Measurement ID.
# Required for analytics locally and in CI (GitHub repo variable of the same name).
# Restart the dev server after creating or editing `.env`.
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
